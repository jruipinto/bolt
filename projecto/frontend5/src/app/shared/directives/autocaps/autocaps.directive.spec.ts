import { AutocapsDirective } from './autocaps.directive';

describe('AutocapsDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocapsDirective();
    expect(directive).toBeTruthy();
  });
});
