import { AutofixCommaDirective } from './autofix-comma.directive';

describe('AutofixCommaDirective', () => {
  it('should create an instance', () => {
    const directive = new AutofixCommaDirective();
    expect(directive).toBeTruthy();
  });
});
