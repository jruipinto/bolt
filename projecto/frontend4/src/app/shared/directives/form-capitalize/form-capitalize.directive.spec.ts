import { FormCapitalizeDirective } from './form-capitalize.directive';

describe('FormCapitalizeDirective', () => {
  it('should create an instance', () => {
    const directive = new FormCapitalizeDirective();
    expect(directive).toBeTruthy();
  });
});
